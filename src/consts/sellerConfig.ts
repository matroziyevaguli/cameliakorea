// Seller-facing help/contact config (NOT secrets — safe on the client).
// Edit these values here rather than hardcoding them in components.
export const SELLER_CONFIG = {
  // Where "Yordam" (Help) sends the seller. Use your real admin contact.
  adminTelegramUrl:    'https://t.me/cameliakorea',
  adminTelegramHandle: '@cameliakorea',
  adminPhone:          '+998940994499',        // tel: link (no spaces)
  adminPhoneDisplay:   '+998 94 099 44 99',
  // Optional 1-minute "how to record a sale" video. Empty = hide the row.
  helpVideoUrl:        '',
}
